# Expo + React Native need a flat node_modules for autolinking and Metro to find
# native modules and hoisted peers. pnpm's default isolated store breaks both.
node-linker=hoisted

# npm fallback (ignored by pnpm): React 19 / RN 0.81 peer graph is strict.
legacy-peer-deps=true
